{
  "name": "ta-toolkit-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper (Capacitor) for the existing TA Toolkit PWA. Does not affect the web/PWA version.",
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.2"
  }
}
